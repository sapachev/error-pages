{{#codes}}
error_page {{.}} = @error{{.}};
{{/codes}}

{{#codes}}
location @error{{.}} {
  root /usr/share/nginx/html/error-pages;
  allow all;
  auth_basic off;
  internal;
  try_files $uri /{{.}}.html ={{.}};
}
{{/codes}}